#include <stdio.h>
#include <locale.h>
#define t 50

void inverter(const char *entrada, *invertido){
    
for(int i=0; i<tamanho_string;i++ ){

    


}


}

int main(){


setlocale(LC_ALL,"portuguese");
char entrada[t],invertido[t];

printf("Digite a palavra que deseja ver se é palindroma");
gets(entrada);
tamanho_string = strlen(entrada);

inverter(entrada,invertido);

}